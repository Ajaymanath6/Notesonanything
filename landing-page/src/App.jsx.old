import { motion } from 'framer-motion'
import { 
  ArrowRight, 
  Check, 
  MapPin, 
  MessageSquare, 
  Users, 
  Zap, 
  Target, 
  Globe, 
  Code, 
  Palette, 
  Building, 
  BarChart3,
  Clock,
  Shield,
  Star,
  ChevronRight,
  Play,
  TrendingDown,
  AlertTriangle,
  Timer
} from 'lucide-react'

// NOA Logo - Simplified
const NOALogo = ({ className = "h-8 w-8" }) => (
  <div className={`${className} relative`}>
    <div className="absolute inset-0 bg-gray-900 rounded-lg"></div>
    <div className="relative bg-white rounded-lg p-1.5 border border-gray-300">
      <div className="text-gray-900 font-bold text-sm">N</div>
    </div>
  </div>
)

// Enhanced Cost Statistics Component
const CostStatistic = ({ icon, number, unit, label, description, delay = 0 }) => (
  <motion.div 
    className="group relative"
    initial={{ opacity: 0, y: 30 }}
    whileInView={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.8, delay }}
    viewport={{ once: true }}
  >
    <div className="p-8 rounded-2xl border backdrop-blur-sm hover:shadow-lg transition-all duration-300 h-full bg-white border-gray-200">
      {/* Icon and Visual Element */}
      <div className="flex items-center justify-between mb-6">
        <div className="p-3 rounded-xl bg-gray-100">
          {icon}
        </div>
        <div className="w-12 h-1 bg-gradient-to-r from-gray-600 to-gray-400 rounded-full"></div>
      </div>
      
      {/* Main Statistic */}
      <div className="mb-4">
        <div className="flex items-baseline space-x-1">
          <motion.span 
            className="text-5xl font-bold text-gray-900"
            initial={{ scale: 0.5 }}
            whileInView={{ scale: 1 }}
            transition={{ duration: 0.6, delay: delay + 0.2 }}
            viewport={{ once: true }}
          >
            {number}
          </motion.span>
          <span className="text-xl font-medium text-gray-700">{unit}</span>
        </div>
        <h3 className="text-lg font-semibold text-gray-900 mt-2">{label}</h3>
      </div>
      
      {/* Description */}
      <p className="text-gray-600 text-sm leading-relaxed">{description}</p>
      
      {/* Bottom accent */}
      <div className="mt-6 pt-4 border-t border-gray-100">
        <div className="flex items-center text-xs text-gray-500">
          <TrendingDown className="h-3 w-3 mr-1" />
          <span>Impact on productivity</span>
        </div>
      </div>
    </div>
  </motion.div>
)

// Enhanced Problem Visualization Component with Better Design
const EnhancedProblemFlow = () => (
  <motion.div 
    className="max-w-6xl mx-auto mt-16 relative"
    initial={{ opacity: 0 }}
    whileInView={{ opacity: 1 }}
    transition={{ duration: 1, delay: 0.5 }}
    viewport={{ once: true }}
  >
    {/* Simplified Lightning Effect Container */}
    <div className="w-screen relative left-1/2 transform -translate-x-1/2 mb-12">
      <div className="flex items-center justify-center">
        
        {/* Left Lightning Divider */}
        <div className="flex-1 relative">
          <div className="w-full relative h-8">
            <div 
              className="absolute inset-0 w-full mt-3"
              style={{
                height: '2px',
                background: 'linear-gradient(90deg, transparent 0%, rgba(75, 85, 99, 0.1) 15%, rgba(75, 85, 99, 0.4) 40%, rgba(75, 85, 99, 0.8) 70%, rgba(75, 85, 99, 1) 90%, rgba(75, 85, 99, 1) 100%)',
                boxShadow: '0 0 4px rgba(75, 85, 99, 0.3)',
                clipPath: 'polygon(0% 50%, 10% 50%, 20% 45%, 30% 55%, 40% 40%, 50% 60%, 60% 35%, 70% 65%, 80% 30%, 90% 70%, 95% 45%, 100% 50%)'
              }}
            />
            <div 
              className="absolute inset-0 w-full"
              style={{
                height: '12px',
                background: 'linear-gradient(90deg, transparent 0%, rgba(75, 85, 99, 0.02) 30%, rgba(75, 85, 99, 0.08) 60%, rgba(75, 85, 99, 0.2) 85%, rgba(75, 85, 99, 0.3) 95%, rgba(75, 85, 99, 0.4) 100%)',
                filter: 'blur(2px)'
              }}
            />
          </div>
        </div>

        {/* Central Crisis Symbol */}
        <div className="flex-shrink-0 px-8">
          <motion.div 
            className="w-16 h-16 bg-gray-600 rounded-full flex items-center justify-center shadow-lg relative"
            animate={{ scale: [1, 1.05, 1] }}
            transition={{ duration: 2, repeat: Infinity }}
          >
            <AlertTriangle className="h-8 w-8 text-white" />
            <div className="absolute -top-1 -right-1 w-1 h-1 bg-gray-300 rounded-full animate-pulse"></div>
            <div className="absolute -bottom-1 -left-1 w-0.5 h-0.5 bg-gray-300 rounded-full animate-pulse" style={{ animationDelay: '0.5s' }}></div>
          </motion.div>
        </div>

        {/* Right Lightning Divider */}
        <div className="flex-1 relative">
          <div className="w-full relative h-8">
            <div 
              className="absolute inset-0 w-full mt-3"
              style={{
                height: '2px',
                background: 'linear-gradient(90deg, rgba(75, 85, 99, 1) 0%, rgba(75, 85, 99, 1) 10%, rgba(75, 85, 99, 0.8) 30%, rgba(75, 85, 99, 0.4) 60%, rgba(75, 85, 99, 0.1) 85%, transparent 100%)',
                boxShadow: '0 0 4px rgba(75, 85, 99, 0.3)',
                clipPath: 'polygon(0% 50%, 5% 45%, 10% 70%, 20% 30%, 30% 65%, 40% 35%, 50% 60%, 60% 40%, 70% 55%, 80% 45%, 90% 50%, 100% 50%)'
              }}
            />
            <div 
              className="absolute inset-0 w-full"
              style={{
                height: '12px',
                background: 'linear-gradient(90deg, rgba(75, 85, 99, 0.4) 0%, rgba(75, 85, 99, 0.3) 5%, rgba(75, 85, 99, 0.2) 15%, rgba(75, 85, 99, 0.08) 40%, rgba(75, 85, 99, 0.02) 70%, transparent 100%)',
                filter: 'blur(2px)'
              }}
            />
          </div>
        </div>

      </div>
    </div>

    {/* The Story: Feedback Communication Breakdown */}
    <div className="bg-white p-8 rounded-2xl border border-gray-200 shadow-md">
      <h3 className="text-2xl font-semibold text-gray-900 mb-8 text-center">
        The Feedback Communication Breakdown
      </h3>
      
      {/* Interactive Story Timeline */}
      <div className="grid md:grid-cols-4 gap-6">
        {[
          { 
            step: "1", 
            title: "Vague Report", 
            desc: '"Something&rsquo;s broken"', 
            icon: <AlertTriangle className="h-6 w-6 text-gray-600" />,
            detail: "Designer sends screenshot with red circle"
          },
          { 
            step: "2", 
            title: "Back & Forth", 
            desc: "40+ messages", 
            icon: <MessageSquare className="h-6 w-6 text-gray-600" />,
            detail: "Developer asks 15 clarifying questions"
          },
          { 
            step: "3", 
            title: "Context Switch", 
            desc: "23 min lost", 
            icon: <Timer className="h-6 w-6 text-gray-600" />,
            detail: "Developer loses flow, productivity drops"
          },
          { 
            step: "4", 
            title: "Frustration", 
            desc: "Team burnout", 
            icon: <TrendingDown className="h-6 w-6 text-gray-600" />,
            detail: "Team morale decreases, quality suffers"
          }
        ].map((item, index) => (
          <motion.div 
            key={index}
            className="text-center relative group cursor-pointer"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.8 + index * 0.2 }}
            viewport={{ once: true }}
            whileHover={{ y: -3 }}
          >
            {/* Step Number */}
            <motion.div 
              className="w-12 h-12 bg-white border-2 border-gray-300 rounded-full flex items-center justify-center mx-auto mb-4 font-bold text-gray-700 shadow-sm group-hover:shadow-md transition-all duration-300"
              animate={{ 
                scale: [1, 1.02, 1] 
              }}
              transition={{ 
                duration: 3 + index * 0.5, 
                repeat: Infinity, 
                delay: index * 0.3 
              }}
            >
              {item.step}
            </motion.div>
            
            {/* Content Card */}
            <div className="p-4 bg-white rounded-xl border border-gray-200 shadow-sm transition-all duration-300 group-hover:shadow-md">
              <div className="flex justify-center mb-3">{item.icon}</div>
              <h4 className="font-semibold text-gray-900 text-sm mb-1">{item.title}</h4>
              <p className="text-xs text-gray-600 mb-2">{item.desc}</p>
              <p className="text-xs text-gray-500 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                {item.detail}
              </p>
            </div>
            
            {/* Connecting Line */}
            {index < 3 && (
              <motion.div 
                className="hidden md:block absolute top-6 left-full w-full pointer-events-none"
                initial={{ scaleX: 0 }}
                whileInView={{ scaleX: 1 }}
                transition={{ duration: 0.8, delay: 1.2 + index * 0.2 }}
                viewport={{ once: true }}
              >
                <div className="w-full h-0.5 bg-gray-300 relative origin-left">
                  <div className="absolute right-0 top-1/2 transform -translate-y-1/2 w-1.5 h-1.5 bg-gray-400 rotate-45"></div>
                </div>
              </motion.div>
            )}
          </motion.div>
        ))}
      </div>

      {/* Bottom Impact Summary */}
      <motion.div 
        className="mt-8 text-center p-6 bg-gray-50 rounded-xl border border-gray-100"
        initial={{ opacity: 0, scale: 0.95 }}
        whileInView={{ opacity: 1, scale: 1 }}
        transition={{ duration: 0.8, delay: 2 }}
        viewport={{ once: true }}
      >
        <div className="flex items-center justify-center space-x-8 text-sm">
          <div className="flex items-center space-x-2">
            <Clock className="h-4 w-4 text-gray-600" />
            <span className="font-semibold text-gray-900">23 min</span>
            <span className="text-gray-600">lost per issue</span>
          </div>
          <div className="flex items-center space-x-2">
            <TrendingDown className="h-4 w-4 text-gray-600" />
            <span className="font-semibold text-gray-900">$127k</span>
            <span className="text-gray-600">annual cost</span>
          </div>
          <div className="flex items-center space-x-2">
            <AlertTriangle className="h-4 w-4 text-gray-600" />
            <span className="font-semibold text-gray-900">68%</span>
            <span className="text-gray-600">project delays</span>
          </div>
        </div>
      </motion.div>
    </div>
  </motion.div>
)

// Sleek Note Design Component - Apple-inspired floating note
const SleekNote = ({ 
  content,
  author,
  avatar,
  timestamp,
  position,
  tone,
  isAiEnhanced = false,
  delay = 0 
}) => (
  <motion.div 
    className="absolute z-20"
    style={{ left: position.x, top: position.y }}
    initial={{ opacity: 0, scale: 0.9, y: 20 }}
    whileInView={{ opacity: 1, scale: 1, y: 0 }}
    transition={{ duration: 0.6, delay }}
    viewport={{ once: true }}
  >
    <div className="relative bg-white rounded-xl p-4 shadow-lg border border-gray-200 w-80">
      {/* Note Header */}
      <div className="px-4 py-3 bg-gradient-to-r from-gray-50 to-white border-b border-gray-100">
        <div className="flex items-center justify-between">
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-gradient-to-br from-gray-600 to-gray-700 rounded-full flex items-center justify-center text-white text-sm font-semibold shadow-sm">
              {avatar}
            </div>
            <div>
              <div className="text-sm font-semibold text-gray-900">{author}</div>
              <div className="text-xs text-gray-500">{timestamp}</div>
            </div>
          </div>
          
          {/* Status Indicator */}
          <div className="flex items-center space-x-2">
            {isAiEnhanced && (
              <div className="px-2 py-1 bg-gray-100 text-gray-700 text-xs font-medium rounded-full flex items-center space-x-1">
                <Zap className="h-3 w-3" />
                <span>AI</span>
              </div>
            )}
            <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
          </div>
        </div>
      </div>
      
      {/* Note Content */}
      <div className="p-4">
        <p className="text-gray-800 text-sm leading-relaxed mb-4">
          {content}
        </p>
        
        {/* AI Enhancement Options */}
        {isAiEnhanced && (
          <div className="space-y-3 mb-4">
            <div className="flex items-center justify-between text-xs text-gray-600">
              <span>Tone: {tone}</span>
              <button className="text-gray-700 hover:text-gray-900 font-medium">Adjust</button>
            </div>
            
            <div className="flex flex-wrap gap-2">
              <button className="px-3 py-1.5 bg-gray-100 hover:bg-gray-200 text-gray-700 text-xs rounded-full transition-colors flex items-center space-x-1">
                <Palette className="h-3 w-3" />
                <span>Rephrase</span>
              </button>
              <button className="px-3 py-1.5 bg-gray-100 hover:bg-gray-200 text-gray-700 text-xs rounded-full transition-colors flex items-center space-x-1">
                <MessageSquare className="h-3 w-3" />
                <span>Complete</span>
              </button>
            </div>
          </div>
        )}
        
        {/* Action Buttons */}
        <div className="flex items-center justify-between pt-3 border-t border-gray-100">
          <div className="flex items-center space-x-3">
            <button className="text-gray-400 hover:text-gray-600 transition-colors">
              <MessageSquare className="h-4 w-4" />
            </button>
            <button className="text-gray-400 hover:text-red-500 transition-colors">
              <Target className="h-4 w-4" />
            </button>
          </div>
          
          <div className="flex items-center space-x-2">
            <button className="px-3 py-1.5 text-gray-600 hover:text-gray-800 text-xs font-medium transition-colors">
              Reply
            </button>
            <button className="px-3 py-1.5 bg-gray-900 hover:bg-black text-white text-xs font-medium rounded-lg transition-colors">
              Resolve
            </button>
          </div>
        </div>
      </div>
    </div>
    
    {/* Floating Pin Indicator */}
    <div className="absolute -top-2 -left-2 w-6 h-6 bg-gray-700 rounded-full flex items-center justify-center shadow-lg shadow-gray-700/30 border-2 border-white">
      <MapPin className="h-3 w-3 text-white" />
    </div>
  </motion.div>
)

// Enhanced Interactive Demo Component - Showcasing Spatial Feedback
const InteractiveDemo = () => (
  <div className="relative">
    {/* Main Demo Container */}
    <div className="relative bg-white rounded-3xl p-8 shadow-2xl border border-amber-200 backdrop-blur-sm">
      {/* Browser Window */}
      <div className="relative bg-gray-50 rounded-2xl shadow-xl overflow-hidden border border-gray-200">
        {/* Browser Chrome */}
        <div className="bg-gray-100 px-6 py-4 flex items-center gap-3 border-b border-gray-200">
          <div className="flex gap-2">
            <div className="w-3 h-3 bg-red-400 rounded-full"></div>
            <div className="w-3 h-3 bg-yellow-400 rounded-full"></div>
            <div className="w-3 h-3 bg-green-400 rounded-full"></div>
          </div>
          <div className="bg-white px-4 py-2 rounded-lg text-sm text-gray-600 ml-4 border border-gray-200 flex-1 max-w-md">
            🌐 https://your-website.com/product
          </div>
          <div className="flex items-center gap-2">
            <div className="w-6 h-6 bg-amber-100 rounded flex items-center justify-center">
              <Users className="h-3 w-3 text-amber-600" />
            </div>
            <span className="text-xs text-gray-500">3 collaborators</span>
          </div>
        </div>
        
        {/* Website Content with Annotations */}
        <div className="p-8 bg-white relative min-h-[400px]">
          {/* Header Section */}
          <div className="flex items-center justify-between mb-6">
            <div className="flex items-center space-x-3">
              <div className="w-10 h-10 bg-blue-500 rounded-lg"></div>
              <div>
                <div className="h-4 bg-gray-200 rounded w-24 mb-1"></div>
                <div className="h-3 bg-gray-100 rounded w-16"></div>
              </div>
            </div>
            <div className="h-10 bg-blue-500 rounded-lg w-24"></div>
          </div>
          
          {/* Hero Section with Annotation */}
          <div className="mb-8 relative">
            <div className="h-6 bg-gray-300 rounded w-3/4 mb-3"></div>
            <div className="h-4 bg-gray-200 rounded w-1/2 mb-4"></div>
            <div className="h-12 bg-blue-100 rounded-lg w-32 relative">
              {/* Annotation Point 1 */}
              <motion.div
                className="absolute -top-2 -right-2 w-8 h-8 bg-orange-500 rounded-full flex items-center justify-center cursor-pointer shadow-lg shadow-orange-500/50 border-2 border-white"
                animate={{ scale: [1, 1.2, 1] }}
                transition={{ duration: 2, repeat: Infinity }}
              >
                <MessageSquare className="h-4 w-4 text-white" />
              </motion.div>
              
              {/* Annotation Tooltip */}
              <motion.div
                className="absolute -top-16 left-1/2 transform -translate-x-1/2 bg-amber-900 text-white p-3 rounded-xl shadow-xl text-sm whitespace-nowrap z-10"
                initial={{ opacity: 0, scale: 0.8, y: 10 }}
                animate={{ opacity: 1, scale: 1, y: 0 }}
                transition={{ delay: 1, duration: 0.5 }}
              >
                <div className="font-medium">Sarah Chen</div>
                <div className="text-amber-200 text-xs">Button needs 2px more padding</div>
                <div className="absolute -bottom-1 left-1/2 transform -translate-x-1/2 w-2 h-2 bg-amber-900 rotate-45"></div>
              </motion.div>
            </div>
          </div>
          
          {/* Content Grid with Multiple Annotations */}
          <div className="grid md:grid-cols-3 gap-6 mb-6">
            {[1, 2, 3].map((item, index) => (
              <div key={item} className="relative">
                <div className="bg-gray-50 rounded-lg p-4 border border-gray-200 h-32 relative">
                  <div className="h-4 bg-gray-200 rounded w-3/4 mb-2"></div>
                  <div className="h-3 bg-gray-100 rounded w-1/2 mb-3"></div>
                  <div className="h-8 bg-gray-200 rounded"></div>
                  
                  {/* Different Annotation Types */}
                  {index === 1 && (
                    <>
                      <motion.div
                        className="absolute top-2 right-2 w-6 h-6 bg-blue-500 rounded-full flex items-center justify-center cursor-pointer shadow-lg"
                        initial={{ opacity: 0 }}
                        animate={{ opacity: 1 }}
                        transition={{ delay: 1.5 }}
                      >
                        <Target className="h-3 w-3 text-white" />
                      </motion.div>
                      
                      <motion.div
                        className="absolute -top-12 right-0 bg-blue-600 text-white p-2 rounded-lg shadow-xl text-xs whitespace-nowrap"
                        initial={{ opacity: 0, x: 10 }}
                        animate={{ opacity: 1, x: 0 }}
                        transition={{ delay: 2 }}
                      >
                        <div className="font-medium">Alex Kim</div>
                        <div className="text-blue-200">Image should be 16:9</div>
                      </motion.div>
                    </>
                  )}
                  
                  {index === 2 && (
                    <>
                      <motion.div
                        className="absolute bottom-2 left-2 w-6 h-6 bg-green-500 rounded-full flex items-center justify-center cursor-pointer shadow-lg"
                        initial={{ opacity: 0 }}
                        animate={{ opacity: 1 }}
                        transition={{ delay: 2.5 }}
                      >
                        <Check className="h-3 w-3 text-white" />
                      </motion.div>
                      
                      <motion.div
                        className="absolute -bottom-12 left-0 bg-green-600 text-white p-2 rounded-lg shadow-xl text-xs whitespace-nowrap"
                        initial={{ opacity: 0, y: -10 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ delay: 3 }}
                      >
                        <div className="font-medium">Maya Patel</div>
                        <div className="text-green-200">Perfect alignment ✓</div>
                      </motion.div>
                    </>
                  )}
                </div>
              </div>
            ))}
          </div>
          
          {/* Collaboration Indicators */}
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-3">
              <div className="flex -space-x-2">
                {['bg-orange-500', 'bg-blue-500', 'bg-green-500'].map((color, index) => (
                  <motion.div
                    key={index}
                    className={`w-8 h-8 ${color} rounded-full border-2 border-white flex items-center justify-center text-white text-xs font-medium`}
                    initial={{ scale: 0 }}
                    animate={{ scale: 1 }}
                    transition={{ delay: 0.5 + index * 0.2 }}
                  >
                    {index + 1}
                  </motion.div>
                ))}
              </div>
              <span className="text-sm text-gray-600">3 people reviewing</span>
            </div>
            
            <motion.div
              className="flex items-center space-x-2 text-sm text-gray-500"
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ delay: 3.5 }}
            >
              <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
              <span>Live collaboration</span>
            </motion.div>
          </div>
        </div>
      </div>
      
      {/* Floating Elements */}
      <motion.div
        className="absolute -top-4 -right-4 bg-orange-500 text-white px-4 py-2 rounded-full text-sm font-medium shadow-lg"
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.5 }}
      >
        ✨ Live Demo
      </motion.div>
      
      <motion.div
        className="absolute -bottom-6 -left-6 bg-amber-800 text-white px-4 py-2 rounded-full text-sm font-medium shadow-lg"
        initial={{ opacity: 0, x: -20 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ delay: 4 }}
      >
        🎯 90% Faster Feedback
      </motion.div>
    </div>
    
    {/* Background Decoration */}
    <div className="absolute inset-0 -z-10">
      <div className="absolute top-4 right-8 w-32 h-32 bg-amber-100 rounded-full blur-2xl opacity-60"></div>
      <div className="absolute bottom-8 left-4 w-24 h-24 bg-orange-100 rounded-full blur-2xl opacity-60"></div>
    </div>
  </div>
)

function App() {
  return (
    <div className="min-h-screen bg-white">
      {/* Header */}
      <header className="sticky top-0 z-50 backdrop-blur-md border-b bg-white/95 border-gray-200">
        <nav className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <motion.div 
            className="flex items-center space-x-3"
            initial={{ opacity: 0, x: -20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.5 }}
          >
            <NOALogo className="h-10 w-10" />
            <div>
              <span className="text-2xl font-bold text-gray-900">NOA</span>
              <div className="text-xs -mt-1 text-gray-600">Notes on Anything</div>
            </div>
          </motion.div>
          
          <motion.div 
            className="hidden md:flex items-center space-x-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 0.1 }}
          >
            <a href="#features" className="text-gray-600 hover:text-gray-900 transition-colors font-medium">Features</a>
            <a href="#pricing" className="text-gray-600 hover:text-gray-900 transition-colors font-medium">Pricing</a>
            <button 
              className="bg-gray-900 text-white px-6 py-2.5 rounded-lg hover:bg-black transition-colors font-medium shadow-lg"
            >
              Try Free Forever
            </button>
          </motion.div>
        </nav>
      </header>

      {/* Hero Section */}
      <section className="px-6 py-20 relative overflow-hidden bg-gradient-to-br from-gray-50 via-white to-gray-100">
        <div className="max-w-7xl mx-auto relative z-10">
          <div className="grid lg:grid-cols-2 gap-16 items-center">
            <div>
              <motion.div
                className="inline-flex items-center space-x-2 px-4 py-2 rounded-full text-sm font-medium mb-8 bg-gray-900 text-gray-100 shadow-lg"
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.6 }}
              >
                <Zap className="h-4 w-4" />
                <span>For teams tired of feedback chaos</span>
              </motion.div>
              
              <motion.h1 
                className="text-5xl md:text-6xl font-bold mb-6 leading-tight text-gray-900"
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.2 }}
              >
                &ldquo;Can you be{' '}
                <span className="text-gray-600">more specific?&rdquo;</span>
                <br />
                <span className="bg-gradient-to-r from-gray-900 to-black bg-clip-text text-transparent">
                  Never again.
                </span>
              </motion.h1>
              
              <motion.p 
                className="text-xl mb-10 leading-relaxed text-gray-700"
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.4 }}
              >
                Tired of playing twenty questions with every design review? 
                Show exactly what needs fixing, exactly where it is, exactly why it matters. 
                Turn vague feedback into crystal-clear action.
              </motion.p>
              
              <motion.div 
                className="flex flex-col sm:flex-row gap-4"
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.6 }}
              >
                <button 
                  className="bg-gray-900 text-white px-8 py-4 rounded-xl hover:bg-black transition-colors flex items-center justify-center space-x-2 text-lg font-semibold shadow-lg"
                >
                  <span>Try Free Forever</span>
                  <ArrowRight className="h-5 w-5" />
                </button>
                <button 
                  className="border border-gray-300 text-gray-700 px-8 py-4 rounded-xl hover:bg-gray-50 transition-colors text-lg font-semibold flex items-center space-x-2"
                >
                  <Play className="h-5 w-5" />
                  <span>Watch Demo</span>
                </button>
              </motion.div>
              
              <motion.div
                className="mt-8 flex items-center space-x-6 text-sm text-gray-600"
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ duration: 0.8, delay: 0.8 }}
              >
                <div className="flex items-center space-x-1">
                  <Check className="h-4 w-4 text-green-600" />
                  <span>No setup required</span>
                </div>
                <div className="flex items-center space-x-1">
                  <Check className="h-4 w-4 text-green-600" />
                  <span>Works on any website</span>
                </div>
                <div className="flex items-center space-x-1">
                  <Check className="h-4 w-4 text-green-600" />
                  <span>Free forever plan</span>
                </div>
              </motion.div>
            </div>
            
            {/* Simple Hero Illustration */}
            <motion.div
              className="relative"
              initial={{ opacity: 0, x: 30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.8, delay: 0.4 }}
            >
              <div className="relative bg-gradient-to-br from-white to-gray-50 rounded-3xl p-8 shadow-xl border border-gray-200 min-h-[500px]">
                <div className="absolute inset-0 bg-gray-100/50 rounded-3xl"></div>
                <div className="relative z-10 flex items-center justify-center h-full">
                  <div className="text-center">
                    <div className="text-4xl mb-4">✨</div>
                    <div className="text-gray-600">Interactive Demo Coming Soon</div>
                  </div>
                </div>
              </div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="px-6 py-12 text-gray-600 bg-gray-50">
        <div className="max-w-7xl mx-auto text-center">
          <p>© 2024 NOA. All rights reserved.</p>
        </div>
      </footer>
    </div>
  )
}

export default App
